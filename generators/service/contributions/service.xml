<?xml version="1.0"?>
<component name="<%= package %>.<%= s.camelize(service_name) %>">

  <implementation class="<%= package %>.<%= s.camelize(service_name)%>Impl" />

  <service>
    <provide interface="<%= package %>.<%= s.camelize(service_name) %>" />
  </service>
</component>
